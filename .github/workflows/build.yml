name: Сборка APK

# Когда запускать сборку
on:
  push:  # Когда ты загружаешь новые файлы
    branches: [ main ]  # В главную ветку
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # Можно запустить вручную

# Что делать при сборке
jobs:
  build:
    runs-on: ubuntu-latest  # Используем компьютер Ubuntu в облаке
    
    steps:
    - name: Загружаем код
      uses: actions/checkout@v4  # Тут тоже лучше обновить до v4
      
    - name: Устанавливаем Java
      uses: actions/setup-java@v4  # И здесь обновим до v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    - name: Собираем APK
      run: |
        chmod +x gradlew
        ./gradlew assembleRelease
        
    - name: Загружаем APK
      uses: actions/upload-artifact@v4  # ГЛАВНОЕ ИСПРАВЛЕНИЕ: v3 → v4
      with:
        name: app-release
        path: app/build/outputs/apk/release/*.apk
